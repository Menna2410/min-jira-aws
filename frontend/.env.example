# Backend (local dev — point at your Express API origin, no trailing slash)
VITE_API_BASE_URL=http://localhost:4000

# Cognito app client — must match backend COGNITO_USER_POOL_ID / COGNITO_CLIENT_ID
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
